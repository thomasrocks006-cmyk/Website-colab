import React, { useState, useEffect, useRef } from 'react';
import { 
  ArrowRight, 
  Cpu, 
  Layers, 
  ChevronRight,
  Database,
  Activity,
  Box,
  Zap,
  Shield,
  CircleDot,
  Command,
  Maximize2,
  Wind
} from 'lucide-react';

// --- THE SPLASH CORE (Refined) ---
const SplashCore = ({ stage }) => {
  return (
    <div className={`relative flex items-center justify-center transition-all duration-1000 ease-in-out 
      ${stage === 'transitioning' ? 'scale-[0.1] -translate-y-[44vh] -translate-x-[42vw] opacity-0 md:opacity-100' : 'scale-100'}`}>
      
      {/* Structural Orbitals */}
      <svg viewBox="0 0 200 200" className={`absolute w-80 h-80 transition-opacity duration-700 ${stage === 'transitioning' ? 'opacity-0' : 'opacity-10'}`}>
        <circle cx="100" cy="100" r="80" fill="none" stroke="currentColor" strokeWidth="0.5" strokeDasharray="1 4" />
        <path d="M40 100 A 60 60 0 0 1 160 100" fill="none" stroke="currentColor" strokeWidth="1" />
      </svg>

      {/* The Monolith Logo */}
      <div className="relative z-10 w-32 h-32 md:w-48 md:h-48 bg-white rounded-[24%] shadow-[0_40px_100px_rgba(0,0,0,0.1)] flex items-center justify-center border border-zinc-100 group overflow-hidden">
        <div className="absolute inset-0 bg-gradient-to-br from-white via-zinc-50 to-zinc-100 opacity-50" />
        <span className="relative font-serif text-5xl md:text-7xl font-light tracking-tighter text-zinc-900">
          C<span className="text-zinc-300 italic">C</span>
        </span>
        <div className="absolute top-0 left-[-100%] w-full h-full bg-gradient-to-r from-transparent via-white/40 to-transparent skew-x-12 animate-[shimmer_3s_infinite]" />
      </div>
    </div>
  );
};

const App = () => {
  const [stage, setStage] = useState('splash'); // 'splash' | 'transitioning' | 'ready'
  const [scrolled, setScrolled] = useState(false);
  const [systemMode, setSystemMode] = useState('casper'); // 'camber' or 'casper'

  useEffect(() => {
    const t1 = setTimeout(() => setStage('transitioning'), 2400);
    const t2 = setTimeout(() => setStage('ready'), 3400);

    const handleScroll = () => setScrolled(window.scrollY > 40);
    window.addEventListener('scroll', handleScroll);

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) entry.target.classList.add('active');
      });
    }, { threshold: 0.1 });

    document.querySelectorAll('.scroll-reveal').forEach(el => observer.observe(el));

    return () => {
      clearTimeout(t1);
      clearTimeout(t2);
      window.removeEventListener('scroll', handleScroll);
    };
  }, []);

  return (
    <div className="min-h-screen bg-[#FDFDFD] text-[#1D1D1F] font-sans selection:bg-zinc-900 selection:text-white">
      
      {/* 1. SPLASH LAYER */}
      <div className={`fixed inset-0 z-[100] bg-[#FDFDFD] flex flex-col items-center justify-center transition-opacity duration-1000 
        ${stage === 'ready' ? 'opacity-0 pointer-events-none' : 'opacity-100'}`}>
        <SplashCore stage={stage} />
        <div className={`mt-16 transition-opacity duration-500 ${stage === 'transitioning' ? 'opacity-0' : 'opacity-100'}`}>
          <p className="text-[10px] tracking-[0.6em] uppercase text-zinc-400 font-bold">System Initialization</p>
        </div>
      </div>

      {/* 2. NAVIGATION */}
      <nav className={`fixed top-0 w-full z-50 transition-all duration-700 px-8 ${scrolled ? 'py-4 bg-white/80 backdrop-blur-xl border-b border-zinc-100' : 'py-10 opacity-0 md:opacity-100'}`}>
        <div className="max-w-7xl mx-auto flex justify-between items-center">
          <div className="flex items-center gap-10">
            <div className="flex items-center gap-3 cursor-pointer group">
              <div className="w-8 h-8 bg-white border border-zinc-100 rounded-[24%] shadow-sm flex items-center justify-center text-xs font-serif font-light overflow-hidden">
                C<span className="text-zinc-300">C</span>
              </div>
              <span className="font-serif text-2xl font-light tracking-tighter">C&C</span>
            </div>
            <div className="hidden lg:flex gap-8 text-[10px] font-bold tracking-[0.2em] uppercase text-zinc-400">
              <a href="#" className="hover:text-zinc-900 transition-colors">Core</a>
              <a href="#" className="hover:text-zinc-900 transition-colors">Systems</a>
              <a href="#" className="hover:text-zinc-900 transition-colors">Ironbark</a>
            </div>
          </div>
          <div className="flex items-center gap-6">
            <div className="hidden sm:flex items-center gap-2 text-[9px] font-bold tracking-widest uppercase text-zinc-300">
              <CircleDot size={10} className="text-green-500" /> Latency: 0.8ms
            </div>
            <button className="bg-zinc-900 text-white px-6 py-2.5 rounded-full text-[11px] font-bold tracking-tight hover:bg-zinc-700 transition-all">
              Enterprise Portal
            </button>
          </div>
        </div>
      </nav>

      {/* 3. HERO: THE DUAL IDENTITY */}
      <main className={`transition-opacity duration-1000 pt-20 ${stage === 'ready' ? 'opacity-100' : 'opacity-0'}`}>
        
        <section className="relative min-h-[90vh] flex flex-col items-center justify-center px-6 text-center">
          <div className="max-w-5xl space-y-12">
            <div className="space-y-6 scroll-reveal">
              <div className="inline-flex rounded-full bg-zinc-100 p-1 border border-zinc-200">
                <button 
                  onClick={() => setSystemMode('casper')}
                  className={`px-6 py-1.5 rounded-full text-[10px] font-bold tracking-widest uppercase transition-all 
                  ${systemMode === 'casper' ? 'bg-white shadow-sm text-zinc-900' : 'text-zinc-400'}`}>
                  Casper
                </button>
                <button 
                  onClick={() => setSystemMode('camber')}
                  className={`px-6 py-1.5 rounded-full text-[10px] font-bold tracking-widest uppercase transition-all 
                  ${systemMode === 'camber' ? 'bg-white shadow-sm text-zinc-900' : 'text-zinc-400'}`}>
                  Camber
                </button>
              </div>
              <h1 className="text-6xl md:text-[100px] font-semibold tracking-tighter leading-[0.9] text-zinc-950">
                {systemMode === 'casper' ? (
                  <>The Ghost <br/><span className="text-zinc-300 italic font-light">in the machine.</span></>
                ) : (
                  <>The Strength <br/><span className="text-zinc-300 italic font-light">beneath the lift.</span></>
                )}
              </h1>
            </div>
            
            <p className="text-xl md:text-2xl text-zinc-400 max-w-2xl mx-auto leading-relaxed font-light scroll-reveal delay-100">
              {systemMode === 'casper' 
                ? "Casper is our cognitive interface—the intelligent, effortless layer that translates complex operations into executive action."
                : "Camber is our foundational architecture—the evolved Ironbark hardware that provides the brute compute power for global AI."
              }
            </p>

            <div className="flex flex-col sm:flex-row gap-6 justify-center pt-8 scroll-reveal delay-200">
              <button className="bg-zinc-900 text-white px-12 py-5 rounded-full font-bold text-sm tracking-tight transition-all hover:bg-black hover:scale-105 shadow-2xl shadow-zinc-200">
                Deploy {systemMode === 'casper' ? 'Intelligence' : 'Infrastructure'}
              </button>
              <button className="text-zinc-900 px-12 py-5 rounded-full font-bold text-sm tracking-tight hover:bg-zinc-100 transition-all flex items-center justify-center gap-2">
                Learn More <ChevronRight size={16} />
              </button>
            </div>
          </div>
        </section>

        {/* 4. THE SPLIT SYSTEM (DEPTH SECTION) */}
        <section className="py-20 px-6 max-w-7xl mx-auto">
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-8 h-full min-h-[700px]">
            
            {/* Camber: The Hardware Side */}
            <div className="bg-[#1D1D1F] text-white rounded-[50px] p-16 flex flex-col justify-between overflow-hidden relative group scroll-reveal">
              <div className="absolute top-0 right-0 p-16 opacity-10 group-hover:opacity-30 transition-opacity">
                <Database size={240} strokeWidth={0.5} />
              </div>
              <div className="relative z-10 space-y-6">
                <div className="w-12 h-12 rounded-2xl bg-zinc-800 flex items-center justify-center">
                  <Cpu className="text-zinc-400" />
                </div>
                <h3 className="text-4xl font-semibold tracking-tight uppercase italic">Camber</h3>
                <p className="text-zinc-400 text-lg max-w-xs leading-relaxed">The structural engine. Hardened Ironbark hardware built to withstand the heaviest neural loads.</p>
              </div>
              <div className="relative z-10 grid grid-cols-2 gap-8 pt-12 border-t border-white/5">
                <div>
                  <div className="text-3xl font-light">1.2 PB</div>
                  <div className="text-[10px] uppercase tracking-widest text-zinc-500 font-bold">Daily Throughput</div>
                </div>
                <div>
                  <div className="text-3xl font-light">99.99%</div>
                  <div className="text-[10px] uppercase tracking-widest text-zinc-500 font-bold">Node Integrity</div>
                </div>
              </div>
            </div>

            {/* Casper: The Software Side */}
            <div className="bg-white rounded-[50px] p-16 flex flex-col justify-between border border-zinc-100 shadow-sm relative group overflow-hidden scroll-reveal delay-100">
               <div className="absolute -bottom-10 -right-10 opacity-5 group-hover:opacity-10 transition-opacity">
                 <Wind size={300} strokeWidth={0.5} />
               </div>
               <div className="relative z-10 space-y-6">
                <div className="w-12 h-12 rounded-2xl bg-zinc-900 flex items-center justify-center">
                  <Command className="text-white" />
                </div>
                <h3 className="text-4xl font-semibold tracking-tight uppercase italic">Casper</h3>
                <p className="text-zinc-500 text-lg max-w-xs leading-relaxed">The cognitive spirit. Intelligent orchestration that makes global AI feel as simple as a conversation.</p>
              </div>
              <div className="relative z-10 space-y-8">
                 <div className="flex items-center gap-4">
                    <div className="h-2 w-full bg-zinc-100 rounded-full overflow-hidden">
                       <div className="h-full bg-zinc-900 w-2/3 animate-[shimmer_3s_infinite]" />
                    </div>
                    <span className="text-xs font-bold text-zinc-400">OPTIMIZING</span>
                 </div>
                 <button className="text-zinc-900 font-bold text-sm tracking-tight flex items-center gap-2 group">
                   Access Intelligence Suite <ArrowRight size={16} className="group-hover:translate-x-2 transition-transform" />
                 </button>
              </div>
            </div>

          </div>
        </section>

        {/* 5. THE TRANSITION: IRONBARK INTEGRATION */}
        <section className="py-40 px-6 max-w-5xl mx-auto text-center">
           <div className="space-y-12 scroll-reveal">
              <h2 className="text-xs font-bold tracking-[0.4em] uppercase text-zinc-400">The Evolution</h2>
              <h3 className="text-5xl md:text-7xl font-semibold tracking-tight leading-tight">Ironbark is the <br/> DNA of Camber.</h3>
              <p className="text-xl text-zinc-500 max-w-2xl mx-auto leading-relaxed font-light">
                 We took the unshakeable reliability of Ironbark and gave it a mind. Camber & Casper isn't just a rebrand; it’s the completion of a vision. Brute force meets pure intelligence.
              </p>
              <div className="flex items-center justify-center gap-8 py-10">
                 <div className="text-center space-y-2">
                    <div className="h-[1px] w-20 bg-zinc-200 mx-auto" />
                    <span className="text-[10px] font-bold uppercase tracking-widest text-zinc-400">Physical Nodes</span>
                 </div>
                 <div className="w-12 h-12 rounded-full border border-zinc-200 flex items-center justify-center">
                    <Zap size={16} className="text-zinc-900" />
                 </div>
                 <div className="text-center space-y-2">
                    <div className="h-[1px] w-20 bg-zinc-200 mx-auto" />
                    <span className="text-[10px] font-bold uppercase tracking-widest text-zinc-400">Cognitive Hub</span>
                 </div>
              </div>
           </div>
        </section>

        {/* 6. CALL TO ACTION */}
        <section className="py-60 px-6 text-center bg-[#F5F5F7]">
           <div className="max-w-4xl mx-auto space-y-16 scroll-reveal">
              <h2 className="text-6xl md:text-9xl font-semibold tracking-tighter">Command <br/><span className="text-zinc-300 italic font-light">the Full Stack.</span></h2>
              <div className="flex flex-col sm:flex-row gap-6 justify-center">
                <button className="bg-zinc-900 text-white px-16 py-6 rounded-full font-bold text-sm tracking-tight transition-all hover:scale-105 shadow-2xl">
                   Partner with the Umbrella
                </button>
              </div>
           </div>
        </section>

      </main>

      {/* FOOTER */}
      <footer className="py-20 px-10 border-t border-zinc-100 bg-white">
         <div className="max-w-7xl mx-auto">
            <div className="flex flex-col md:flex-row justify-between items-start gap-20 pb-20">
               <div className="space-y-6">
                  <div className="flex items-center gap-3">
                    <div className="w-8 h-8 bg-zinc-900 rounded-[24%] flex items-center justify-center text-white text-[10px] font-serif">
                      C<span className="text-zinc-500">C</span>
                    </div>
                    <span className="font-serif text-2xl tracking-tighter">C&C</span>
                  </div>
                  <p className="text-[10px] tracking-[0.4em] uppercase text-zinc-300 max-w-xs font-bold leading-loose">
                    Structural Infrastructure and Cognitive AI Umbrella Systems.
                  </p>
               </div>
               <div className="grid grid-cols-2 lg:grid-cols-3 gap-20 text-[10px] font-bold tracking-widest uppercase text-zinc-400">
                  <div className="space-y-6">
                     <h5 className="text-zinc-900 font-black">Architecture</h5>
                     <ul className="space-y-3">
                        <li><a href="#" className="hover:text-zinc-900 transition-colors">Camber Engine</a></li>
                        <li><a href="#" className="hover:text-zinc-900 transition-colors">Ironbark Core</a></li>
                        <li><a href="#" className="hover:text-zinc-900 transition-colors">Network Nodes</a></li>
                     </ul>
                  </div>
                  <div className="space-y-6">
                     <h5 className="text-zinc-900 font-black">Intelligence</h5>
                     <ul className="space-y-3">
                        <li><a href="#" className="hover:text-zinc-900 transition-colors">Casper AI</a></li>
                        <li><a href="#" className="hover:text-zinc-900 transition-colors">Neural Logic</a></li>
                        <li><a href="#" className="hover:text-zinc-900 transition-colors">Synthesis UI</a></li>
                     </ul>
                  </div>
               </div>
            </div>
            <div className="pt-10 border-t border-zinc-50 flex flex-col md:flex-row justify-between items-center gap-6 text-[10px] font-bold uppercase tracking-[0.5em] text-zinc-300">
               <span>&copy; 2024 C&C Umbrella Group.</span>
               <div className="flex gap-10">
                  <a href="#" className="hover:text-zinc-900">Security</a>
                  <a href="#" className="hover:text-zinc-900">Privacy</a>
                  <a href="#" className="hover:text-zinc-900">Ethics</a>
               </div>
            </div>
         </div>
      </footer>

      <style dangerouslySetInnerHTML={{ __html: `
        @keyframes shimmer {
          0% { transform: translateX(-100%) skewX(12deg); }
          100% { transform: translateX(200%) skewX(12deg); }
        }
        .scroll-reveal {
          opacity: 0;
          transform: translateY(30px);
          transition: all 1.2s cubic-bezier(0.16, 1, 0.3, 1);
        }
        .scroll-reveal.active {
          opacity: 1;
          transform: translateY(0);
        }
        .delay-100 { transition-delay: 100ms; }
        .delay-200 { transition-delay: 200ms; }
      `}} />
    </div>
  );
};

export default App;