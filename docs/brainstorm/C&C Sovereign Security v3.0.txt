import React, { useState, useEffect, useMemo } from 'react';
import { 
  ShieldCheck, 
  MapPin, 
  Lock, 
  ShieldAlert, 
  CheckCircle2, 
  ArrowRight, 
  Globe, 
  Database, 
  ChevronRight,
  Fingerprint,
  Cpu,
  Zap,
  HardDrive,
  EyeOff,
  Activity
} from 'lucide-react';

// --- HIGH-FIDELITY AUSTRALIA NIGHT MAP ---
const AustraliaNightMap = ({ activeNode, setActiveNode }) => {
  const nodes = [
    { id: 'syd', city: 'Sydney', pos: { x: 84, y: 78 }, status: 'Operational', latency: '0.4ms', load: '22%' },
    { id: 'mel', city: 'Melbourne', pos: { x: 76, y: 88 }, status: 'Operational', latency: '0.8ms', load: '45%' },
    { id: 'per', city: 'Perth', pos: { x: 18, y: 68 }, status: 'Operational', latency: '1.2ms', load: '12%' },
    { id: 'bri', city: 'Brisbane', pos: { x: 88, y: 58 }, status: 'Operational', latency: '0.9ms', load: '31%' },
  ];

  // Simulated "City Lights" (non-server population hubs)
  const cityLights = [
    { x: 82, y: 72 }, { x: 85, y: 79 }, { x: 77, y: 86 }, { x: 74, y: 89 },
    { x: 19, y: 67 }, { x: 21, y: 69 }, { x: 89, y: 57 }, { x: 55, y: 85 },
    { x: 42, y: 82 }, { x: 91, y: 45 }, { x: 81, y: 35 }, { x: 25, y: 32 }
  ];

  return (
    <div className="relative w-full aspect-[4/3] bg-[#020203] rounded-[48px] border border-white/5 overflow-hidden shadow-2xl group cursor-crosshair">
      {/* Atmosphere Glow */}
      <div className="absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,_#1e293b_0%,_transparent_70%)] opacity-20" />
      
      {/* The Map Canvas */}
      <svg viewBox="0 0 100 80" className="w-full h-full p-8 md:p-16">
        <defs>
          <filter id="cityGlow">
            <feGaussianBlur stdDeviation="0.4" result="blur" />
            <feComposite in="SourceGraphic" in2="blur" operator="over" />
          </filter>
          <filter id="nodeGlow">
            <feGaussianBlur stdDeviation="1.5" result="blur" />
            <feComposite in="SourceGraphic" in2="blur" operator="over" />
          </filter>
        </defs>

        {/* Australia Path - Hand-crafted minimal silhouette */}
        <path 
          d="M18,55 L22,65 L32,75 L45,78 L65,78 L80,75 L88,65 L92,50 L88,35 L75,22 L60,18 L45,18 L30,22 L20,35 L16,48 Z" 
          fill="#0a0a0c" 
          stroke="#1e293b" 
          strokeWidth="0.2" 
        />

        {/* Ambient City Lights (The "Night" effect) */}
        {cityLights.map((light, i) => (
          <circle 
            key={`light-${i}`} 
            cx={light.x} cy={light.y} r="0.3" 
            fill="#fbbf24" 
            opacity="0.4" 
            filter="url(#cityGlow)"
          >
            <animate attributeName="opacity" values="0.2;0.5;0.2" dur={`${2 + i % 3}s`} repeatCount="indefinite" />
          </circle>
        ))}

        {/* Ironbark Network Lines (The "Camber" Backbone) */}
        <g opacity="0.1">
          <line x1="84" y1="78" x2="76" y2="88" stroke="#3b82f6" strokeWidth="0.1" />
          <line x1="84" y1="78" x2="88" y2="58" stroke="#3b82f6" strokeWidth="0.1" />
          <line x1="76" y1="88" x2="18" y2="68" stroke="#3b82f6" strokeWidth="0.1" strokeDasharray="1 1" />
        </g>

        {/* Active Node Beacons */}
        {nodes.map(node => (
          <g 
            key={node.id} 
            className="cursor-pointer"
            onMouseEnter={() => setActiveNode(node)}
            onMouseLeave={() => setActiveNode(null)}
          >
            {/* Outer Pulse */}
            <circle cx={node.pos.x} cy={node.pos.y} r="2" fill="#3b82f6" opacity="0.1">
              <animate attributeName="r" values="1.5;4;1.5" dur="3s" repeatCount="indefinite" />
              <animate attributeName="opacity" values="0.2;0;0.2" dur="3s" repeatCount="indefinite" />
            </circle>
            
            {/* Inner Core */}
            <circle 
              cx={node.pos.x} cy={node.pos.y} 
              r={activeNode?.id === node.id ? 1.2 : 0.8} 
              fill={activeNode?.id === node.id ? "#60a5fa" : "#3b82f6"}
              filter="url(#nodeGlow)"
              className="transition-all duration-300"
            />
            
            {/* Label */}
            <text 
              x={node.pos.x} y={node.pos.y + 4} 
              textAnchor="middle" 
              className={`text-[1.5px] font-bold uppercase tracking-widest fill-zinc-500 transition-opacity duration-300 pointer-events-none
                ${activeNode?.id === node.id ? 'opacity-100' : 'opacity-40'}`}
            >
              {node.city}
            </text>
          </g>
        ))}
      </svg>

      {/* Interface Overlays */}
      <div className="absolute top-10 left-10 p-6 bg-black/40 backdrop-blur-md border border-white/5 rounded-3xl space-y-4">
        <div className="flex items-center gap-3">
          <div className="w-2 h-2 bg-blue-500 rounded-full animate-pulse" />
          <span className="text-[10px] font-mono text-white/60 tracking-widest uppercase font-bold">Ironbark_Sovereign_Link</span>
        </div>
        <div className="space-y-1">
          <div className="text-[9px] text-zinc-500 uppercase font-bold tracking-widest">Active Infrastructure</div>
          <div className="text-xl font-bold text-white tracking-tighter">Australia Solo-Host</div>
        </div>
      </div>

      <div className="absolute bottom-10 right-10 text-right">
        <div className="text-[10px] font-mono text-zinc-600 uppercase tracking-[0.4em] font-bold">Protocol v3.0</div>
        <div className="text-sm font-serif italic text-white/30 tracking-widest">Air-Gapped Environment</div>
      </div>

      {/* Node Details Tooltip (Modern UI) */}
      {activeNode && (
        <div className="absolute top-10 right-10 w-64 bg-white/90 backdrop-blur-2xl border border-white rounded-3xl p-6 shadow-2xl animate-in fade-in zoom-in duration-300 text-zinc-900">
           <div className="flex justify-between items-start mb-4">
              <div className="space-y-1">
                <div className="text-[10px] font-bold uppercase tracking-widest text-blue-600">{activeNode.city} Cluster</div>
                <div className="text-xl font-bold tracking-tighter">Node_ID: {activeNode.id.toUpperCase()}</div>
              </div>
              <Activity size={18} className="text-blue-500" />
           </div>
           <div className="grid grid-cols-2 gap-4">
              <div className="space-y-1">
                 <div className="text-[9px] font-bold uppercase tracking-widest text-zinc-400">Latency</div>
                 <div className="text-sm font-bold">{activeNode.latency}</div>
              </div>
              <div className="space-y-1">
                 <div className="text-[9px] font-bold uppercase tracking-widest text-zinc-400">Load</div>
                 <div className="text-sm font-bold">{activeNode.load}</div>
              </div>
           </div>
           <div className="mt-6 pt-4 border-t border-zinc-200 flex items-center gap-2">
              <CheckCircle2 size={12} className="text-green-600" />
              <span className="text-[9px] font-bold uppercase tracking-widest text-zinc-500">Secured via Camber Shield</span>
           </div>
        </div>
      )}
    </div>
  );
};

// --- MAIN APPLICATION ---

export default function App() {
  const [stage, setStage] = useState('splash');
  const [isScrolled, setIsScrolled] = useState(false);
  const [activeNode, setActiveNode] = useState(null);

  useEffect(() => {
    const timer = setTimeout(() => setStage('main'), 2800);
    const scrollHandler = () => setIsScrolled(window.scrollY > 40);
    window.addEventListener('scroll', scrollHandler);
    return () => {
      clearTimeout(timer);
      window.removeEventListener('scroll', scrollHandler);
    };
  }, []);

  if (stage === 'splash') {
    return (
      <div className="fixed inset-0 bg-white flex flex-col items-center justify-center z-[100] transition-opacity duration-1000">
        <div className="relative w-64 h-64 flex items-center justify-center animate-in zoom-in duration-1000">
          <svg viewBox="0 0 200 200" className="absolute w-full h-full opacity-10">
            <path d="M40 120C40 86.8629 66.8629 60 100 60C133.137 60 160 86.8629 160 120" stroke="black" strokeWidth="1.5" fill="none" />
          </svg>
          <div className="w-32 h-32 bg-white rounded-[24%] shadow-2xl flex items-center justify-center border border-zinc-100 overflow-hidden">
             <span className="font-serif text-5xl font-light tracking-tighter text-zinc-900">C<span className="text-zinc-300 italic">C</span></span>
          </div>
        </div>
        <p className="mt-12 text-[10px] tracking-[0.8em] uppercase text-zinc-400 font-bold animate-pulse">Establishing Sovereign Link</p>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-[#FDFDFD] text-[#1D1D1F] font-sans selection:bg-zinc-900 selection:text-white transition-opacity duration-1000">
      
      {/* Navigation */}
      <nav className={`fixed top-0 w-full z-50 transition-all duration-700 px-8 ${isScrolled ? 'py-4 bg-white/70 backdrop-blur-xl border-b border-zinc-100 shadow-sm' : 'py-10'}`}>
        <div className="max-w-7xl mx-auto flex justify-between items-center">
          <div className="flex items-center gap-10">
            <div className="flex items-center gap-3 cursor-pointer group">
              <div className="w-8 h-8 bg-zinc-950 rounded-[24%] flex items-center justify-center text-white text-[10px] font-serif transition-transform group-hover:rotate-12 shadow-xl">
                C<span className="text-zinc-500">C</span>
              </div>
              <span className="font-serif text-2xl font-light tracking-tighter">C&C</span>
            </div>
            <div className="hidden lg:flex gap-10 text-[9px] font-bold tracking-[0.3em] uppercase text-zinc-400">
              <a href="#map" className="hover:text-zinc-900 transition-colors">The Map</a>
              <a href="#security" className="hover:text-zinc-900 transition-colors">Infrastructure</a>
              <a href="#legacy" className="hover:text-zinc-900 transition-colors">Legacy</a>
            </div>
          </div>
          <button className="bg-zinc-900 text-white px-8 py-3 rounded-full text-[11px] font-bold tracking-tight hover:bg-zinc-700 transition-all shadow-xl shadow-zinc-100">
            Client Terminal
          </button>
        </div>
      </nav>

      <main className="pt-24">
        {/* HERO */}
        <section className="min-h-[85vh] flex flex-col items-center justify-center px-6 text-center max-w-5xl mx-auto space-y-12">
          <div className="space-y-8">
            <div className="inline-block px-4 py-1.5 rounded-full bg-blue-50 text-[10px] font-bold tracking-[0.5em] uppercase text-blue-600 border border-blue-100">
              100% Australian Jurisdiction
            </div>
            <h1 className="text-6xl md:text-[115px] font-semibold tracking-tighter leading-[0.9] text-zinc-950">
              Sovereign Power. <br/>
              <span className="text-zinc-300 italic font-light">On our soil.</span>
            </h1>
            <p className="text-xl md:text-2xl text-zinc-400 max-w-2xl mx-auto font-light leading-relaxed">
              Camber & Casper’s intelligence layer runs exclusively on private Ironbark nodes hosted in Australia. Your data never crosses the border.
            </p>
          </div>
          <div className="flex flex-col sm:flex-row gap-6 justify-center pt-8">
            <button className="bg-zinc-900 text-white px-12 py-5 rounded-full font-bold text-sm tracking-tight transition-all hover:scale-105 shadow-2xl flex items-center gap-3">
              Request Full Audit <ArrowRight size={18} />
            </button>
            <button className="bg-white border border-zinc-200 text-zinc-900 px-12 py-5 rounded-full font-bold text-sm tracking-tight hover:bg-zinc-50 transition-all">
              Security Protocol
            </button>
          </div>
        </section>

        {/* NIGHT MAP SECTION: THE POWER OF INFRASTRUCTURE */}
        <section id="map" className="py-20 px-6 max-w-7xl mx-auto">
          <AustraliaNightMap activeNode={activeNode} setActiveNode={setActiveNode} />
          <div className="mt-12 grid grid-cols-1 md:grid-cols-3 gap-12 text-center max-w-4xl mx-auto">
            <div className="space-y-2">
              <h4 className="text-[10px] font-bold uppercase tracking-[0.4em] text-zinc-300">Data Location</h4>
              <p className="text-lg font-semibold">Australia Only</p>
              <p className="text-xs text-zinc-400 font-light leading-relaxed px-4">Subject only to Australian law. Zero data training in global pools.</p>
            </div>
            <div className="space-y-2 border-x border-zinc-100">
              <h4 className="text-[10px] font-bold uppercase tracking-[0.4em] text-zinc-300">Hardware Layer</h4>
              <p className="text-lg font-semibold">Private Ironbark</p>
              <p className="text-xs text-zinc-400 font-light leading-relaxed px-4">We own the racks. We touch the hardware. No third-party clouds.</p>
            </div>
            <div className="space-y-2">
              <h4 className="text-[10px] font-bold uppercase tracking-[0.4em] text-zinc-300">Intelligence</h4>
              <p className="text-lg font-semibold">Air-Gapped</p>
              <p className="text-xs text-zinc-400 font-light leading-relaxed px-4">Physically separated processing nodes for pure business privacy.</p>
            </div>
          </div>
        </section>

        {/* SECTION: BEYOND THE CLOUD */}
        <section id="security" className="py-40 px-8 max-w-7xl mx-auto">
          <div className="grid grid-cols-1 md:grid-cols-2 gap-24 items-center">
            <div className="space-y-10">
              <h2 className="text-sm font-bold tracking-[0.5em] uppercase text-zinc-400">The Camber Shield</h2>
              <h3 className="text-5xl md:text-7xl font-semibold tracking-tighter leading-tight">Hardened. <br/><span className="text-zinc-200 italic font-light">By Design.</span></h3>
              <p className="text-xl text-zinc-500 font-light leading-relaxed">
                We believe that power without security is a liability. Camber & Casper isn't just an app; it's a hardened environment where your business secrets are protected by physical separation and domestic jurisdiction.
              </p>
              <ul className="space-y-6 pt-6">
                {[
                  { title: "Sovereign Legality", text: "Protected by Australian Privacy Principles." },
                  { title: "Physical Air-Gap", text: "Logic nodes decoupled from public training data." },
                  { title: "Ironbark Integrity", text: "24/7 onsite security at every Australian node." }
                ].map((item, i) => (
                  <li key={i} className="flex items-start gap-4">
                    <div className="mt-1 w-2 h-2 bg-blue-500 rounded-full" />
                    <div>
                       <div className="text-sm font-bold uppercase tracking-widest text-zinc-900">{item.title}</div>
                       <div className="text-xs text-zinc-400 mt-1">{item.text}</div>
                    </div>
                  </li>
                ))}
              </ul>
            </div>
            <div className="relative aspect-square bg-zinc-950 rounded-[60px] p-2 overflow-hidden group shadow-2xl shadow-zinc-200">
               <div className="w-full h-full bg-[#0a0a0c] rounded-[58px] flex items-center justify-center border border-white/5 relative">
                  <div className="absolute inset-0 opacity-20 pointer-events-none">
                     <svg viewBox="0 0 100 100" className="w-full h-full">
                        <circle cx="50" cy="50" r="40" fill="none" stroke="white" strokeWidth="0.1" strokeDasharray="1 4" />
                        <circle cx="50" cy="50" r="30" fill="none" stroke="white" strokeWidth="0.1" strokeDasharray="4 8" />
                     </svg>
                  </div>
                  <div className="relative z-10 text-center">
                     <Lock size={120} className="text-zinc-800 mx-auto" strokeWidth={0.5} />
                     <div className="mt-6 text-[10px] font-mono tracking-[0.8em] uppercase text-zinc-600">Secure Vault Protocol</div>
                  </div>
               </div>
            </div>
          </div>
        </section>

        {/* FINAL IMPACT SECTION */}
        <section className="py-60 px-6 bg-[#020203] text-white text-center relative overflow-hidden">
           <div className="absolute inset-0 opacity-20 pointer-events-none flex items-center justify-center">
              <div className="w-[800px] h-[800px] bg-blue-600/10 rounded-full blur-[120px]" />
           </div>
           <div className="relative z-10 space-y-16 max-w-4xl mx-auto">
              <h2 className="text-6xl md:text-[140px] font-semibold tracking-tighter leading-none">Your Data. <br/><span className="text-zinc-700 italic font-light">Untouchable.</span></h2>
              <p className="text-xl text-zinc-500 font-light max-w-2xl mx-auto leading-relaxed">
                Take the leap into autonomous systems with the confidence that your foundation is built on Australian soil.
              </p>
              <button className="bg-white text-zinc-950 px-16 py-6 rounded-full font-bold text-sm tracking-tight hover:scale-105 transition-all shadow-2xl">
                 Secure Your Organization
              </button>
           </div>
        </section>
      </main>

      <footer className="py-20 px-10 border-t border-zinc-100 bg-white">
        <div className="max-w-7xl mx-auto flex flex-col md:flex-row justify-between items-center gap-10">
          <div className="flex items-center gap-4">
             <div className="w-10 h-10 bg-zinc-900 rounded-[24%] flex items-center justify-center text-white text-xs font-serif italic shadow-lg">C<span className="text-zinc-500">C</span></div>
             <div className="flex flex-col">
                <span className="text-[10px] font-bold uppercase tracking-[0.4em] text-zinc-900">Sovereign Guard</span>
                <span className="text-[8px] uppercase tracking-widest text-zinc-400">C&C Umbrella Protocol</span>
             </div>
          </div>
          <div className="flex gap-12 text-[10px] font-bold uppercase tracking-widest text-zinc-400">
            <a href="#" className="hover:text-blue-600 transition-colors">Infrastructure</a>
            <a href="#" className="hover:text-blue-600 transition-colors">Legal</a>
            <a href="#" className="hover:text-blue-600 transition-colors">Nodes</a>
            <a href="#" className="hover:text-blue-600 transition-colors">Contact</a>
          </div>
          <div className="text-zinc-200 text-[9px] tracking-[0.5em] font-mono">
            © 2024 CAMBER_CASPER_CORE
          </div>
        </div>
      </footer>
    </div>
  );
}