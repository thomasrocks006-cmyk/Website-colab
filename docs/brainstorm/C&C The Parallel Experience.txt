import React, { useState, useEffect } from 'react';
import { 
  Clock, 
  CheckCircle2, 
  XCircle, 
  ArrowRight, 
  Zap, 
  Heart, 
  Coffee, 
  Moon, 
  Sun, 
  PhoneCall, 
  FileText, 
  ShieldCheck,
  Smartphone,
  Calendar
} from 'lucide-react';

const ExperienceCard = ({ time, title, traditional, cc, isActive }) => (
  <div className={`grid grid-cols-1 md:grid-cols-2 gap-0 border-b border-zinc-100 transition-all duration-700 ${isActive ? 'opacity-100' : 'opacity-40 blur-[1px]'}`}>
    {/* Traditional Side */}
    <div className="p-10 border-r border-zinc-100 bg-white group overflow-hidden relative">
       <div className="flex justify-between items-start mb-6">
          <span className="text-[10px] font-mono text-zinc-300 uppercase tracking-widest">{time}</span>
          <XCircle size={18} className="text-zinc-200" />
       </div>
       <h4 className="text-xl font-medium mb-4 text-zinc-400 group-hover:text-zinc-600 transition-colors">{traditional.task}</h4>
       <p className="text-sm text-zinc-400 font-light leading-relaxed italic">"{traditional.pain}"</p>
    </div>

    {/* C&C Side */}
    <div className={`p-10 bg-zinc-50 relative overflow-hidden transition-all duration-700 ${isActive ? 'bg-zinc-900 text-white' : 'bg-zinc-50'}`}>
       <div className="flex justify-between items-start mb-6">
          <span className={`text-[10px] font-mono uppercase tracking-widest ${isActive ? 'text-blue-400' : 'text-zinc-300'}`}>Agent_Action: Executed</span>
          <CheckCircle2 size={18} className={isActive ? 'text-blue-500' : 'text-zinc-200'} />
       </div>
       <h4 className="text-xl font-medium mb-4">{cc.task}</h4>
       <p className={`text-sm leading-relaxed font-light ${isActive ? 'text-zinc-400' : 'text-zinc-400'}`}>{cc.benefit}</p>
       
       {isActive && (
         <div className="absolute -bottom-4 -right-4 opacity-10">
            <Zap size={120} fill="currentColor" />
         </div>
       )}
    </div>
  </div>
);

const App = () => {
  const [stage, setStage] = useState('splash');
  const [activeStep, setActiveStep] = useState(0);

  useEffect(() => {
    const timer = setTimeout(() => setStage('main'), 2500);
    return () => clearTimeout(timer);
  }, []);

  const timeline = [
    {
      time: '08:00 AM',
      traditional: { task: 'Filtering 40+ unread emails & texts', pain: 'I started my day already behind.' },
      cc: { task: 'Casper Morning Synthesis', benefit: '100 agents filtered the noise. 3 hot leads booked, 2 quotes sent, 15 junk items deleted. You just drink your coffee.' }
    },
    {
      time: '12:30 PM',
      traditional: { task: 'Stopping work to field quote requests', pain: 'If I don\'t answer, I lose the job. If I do answer, the work stops.' },
      cc: { task: 'Autonomous Lead Capture', benefit: 'The Swarm handles the intake. Quotes are automatically generated based on your Ironbark price-lists. You stay on the tools.' }
    },
    {
      time: '07:00 PM',
      traditional: { task: 'Drowning in paperwork at the kitchen table', pain: 'I missed dinner again. I’m just a glorified admin clerk now.' },
      cc: { task: 'Sovereign Audit & Close', benefit: 'Invoices chased, bills scheduled, and payroll prepped. Your office is shut. Your evening is yours.' }
    },
    {
      time: '11:00 PM',
      traditional: { task: 'Staring at the ceiling, worrying about safety', pain: 'Is my data safe? Did I pay that insurance?'} ,
      cc: { task: 'Ironbark Silent Sentry', benefit: 'Your air-gapped security nodes are watching. Your reputation is safe on Australian soil. Sleep soundly.' }
    }
  ];

  if (stage === 'splash') {
    return (
      <div className="fixed inset-0 bg-[#FDFDFD] flex flex-col items-center justify-center z-[100]">
        <div className="relative animate-in zoom-in duration-1000">
           <div className="w-24 h-24 bg-zinc-950 rounded-[28%] flex items-center justify-center shadow-2xl">
              <span className="font-serif text-3xl font-light text-white">C<span className="text-zinc-500 italic">C</span></span>
           </div>
        </div>
        <p className="mt-8 text-[10px] tracking-[0.8em] uppercase text-zinc-400 font-bold animate-pulse">Designing Your Day</p>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-[#FDFDFD] text-zinc-900 font-sans selection:bg-zinc-950 selection:text-white transition-opacity duration-1000">
      
      {/* Navigation */}
      <nav className="fixed top-0 w-full z-50 bg-white/70 backdrop-blur-xl border-b border-zinc-100 px-8 py-6">
        <div className="max-w-7xl mx-auto flex justify-between items-center">
          <div className="flex items-center gap-3">
            <div className="w-8 h-8 bg-zinc-950 rounded-xl flex items-center justify-center text-white font-serif text-[10px]">C&C</div>
            <span className="font-serif text-2xl font-light tracking-tighter">Camber & Casper</span>
          </div>
          <button className="bg-zinc-900 text-white px-8 py-2.5 rounded-full text-[11px] font-bold tracking-tight hover:bg-zinc-700 transition-all">
            Join the Swarm
          </button>
        </div>
      </nav>

      <main className="pt-32">
        {/* HERO */}
        <section className="px-6 py-20 max-w-5xl mx-auto text-center space-y-12">
          <div className="space-y-6">
            <div className="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-zinc-100 text-[10px] font-bold tracking-[0.4em] uppercase text-zinc-400 border border-zinc-200">
              The Parallel Life
            </div>
            <h1 className="text-6xl md:text-[100px] font-semibold tracking-tighter leading-[0.9] text-zinc-950">
              One business. <br/>
              <span className="text-zinc-300 italic font-light">Two trajectories.</span>
            </h1>
            <p className="text-xl md:text-2xl text-zinc-400 max-w-2xl mx-auto font-light leading-relaxed">
              Same industry. Same skills. Different results. See how the 100-agent workforce changes the life of the person behind the business.
            </p>
          </div>
        </section>

        {/* INTERACTIVE TIMELINE */}
        <section className="max-w-7xl mx-auto border-t border-zinc-100">
          <div className="bg-white px-10 py-4 flex justify-between items-center border-b border-zinc-100 sticky top-24 z-20">
             <div className="text-[10px] font-bold uppercase tracking-widest text-zinc-400">The Manual Grind</div>
             <div className="flex gap-2">
                {timeline.map((_, i) => (
                  <button 
                    key={i} 
                    onClick={() => setActiveStep(i)}
                    className={`h-1.5 w-8 rounded-full transition-all ${activeStep === i ? 'bg-zinc-900' : 'bg-zinc-100'}`} 
                  />
                ))}
             </div>
             <div className="text-[10px] font-bold uppercase tracking-widest text-blue-600">The C&C Advantage</div>
          </div>

          <div className="flex flex-col">
            {timeline.map((step, index) => (
              <ExperienceCard 
                key={index}
                time={step.time}
                traditional={step.traditional}
                cc={step.cc}
                isActive={activeStep === index}
              />
            ))}
          </div>
        </section>

        {/* MARKETING PUNCH SECTION */}
        <section className="py-40 px-8 max-w-6xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-24 items-center">
           <div className="space-y-8">
              <h2 className="text-xs font-bold tracking-[0.4em] uppercase text-zinc-400">The Unfair Advantage</h2>
              <h3 className="text-5xl font-semibold tracking-tighter leading-[0.95]">Stop being the <br/><span className="text-zinc-300 italic font-light text-zinc-900">Chief Everything Officer.</span></h3>
              <p className="text-lg text-zinc-500 font-light leading-relaxed">
                Most business owners are drowning in the very thing they created. Camber & Casper gives you the breathing room to be the Director while the Swarm acts as your staff.
              </p>
              
              <div className="space-y-6 pt-4">
                 {[
                   { icon: <Heart size={20} className="text-pink-500" />, title: "More Family Time", text: "Dinner at 6 PM. Not 10 PM." },
                   { icon: <Zap size={20} className="text-blue-500" />, title: "Zero Lead Lag", text: "Quotes sent while you sleep." },
                   { icon: <ShieldCheck size={20} className="text-emerald-500" />, title: "Sovereign Peace", text: "Australian data. Zero worries." }
                 ].map((item, i) => (
                   <div key={i} className="flex items-start gap-4">
                      <div className="mt-1">{item.icon}</div>
                      <div>
                         <div className="text-sm font-bold uppercase tracking-widest">{item.title}</div>
                         <div className="text-xs text-zinc-400 mt-1">{item.text}</div>
                      </div>
                   </div>
                 ))}
              </div>
           </div>

           <div className="bg-zinc-950 rounded-[60px] p-2 aspect-square relative overflow-hidden group shadow-2xl">
              <div className="w-full h-full bg-[#0A0A0C] rounded-[58px] flex items-center justify-center border border-white/5 relative">
                 <div className="absolute inset-0 opacity-10">
                    <div className="grid grid-cols-8 h-full gap-2 p-10">
                       {[...Array(24)].map((_, i) => (
                         <div key={i} className="bg-white h-full rounded-sm animate-pulse" style={{ animationDelay: `${i * 0.1}s` }} />
                       ))}
                    </div>
                 </div>
                 <div className="relative z-10 text-center space-y-6">
                    <div className="w-20 h-20 bg-white rounded-3xl mx-auto flex items-center justify-center shadow-2xl">
                       <Smartphone size={32} className="text-zinc-900" />
                    </div>
                    <div className="text-white text-3xl font-bold tracking-tighter">100 Agents <br/> In your pocket.</div>
                    <button className="bg-blue-600 text-white px-8 py-3 rounded-full text-[10px] font-bold uppercase tracking-widest hover:bg-blue-500 transition-all">
                       Claim Your Advantage
                    </button>
                 </div>
              </div>
           </div>
        </section>

        {/* CTA */}
        <section className="py-60 px-6 text-center bg-white border-t border-zinc-100">
           <h2 className="text-5xl md:text-[130px] font-semibold tracking-tighter leading-none mb-16">Choose your <br/><span className="text-zinc-200 italic font-light">trajectory.</span></h2>
           <div className="flex flex-col sm:flex-row gap-6 justify-center">
             <button className="bg-zinc-900 text-white px-16 py-6 rounded-full font-bold text-sm tracking-tight transition-all hover:scale-105 shadow-2xl shadow-zinc-300">
                Hire the Swarm Today
             </button>
           </div>
           <p className="mt-10 text-[10px] font-bold text-zinc-300 uppercase tracking-[0.5em]">The $799 Decision.</p>
        </section>
      </main>

      <footer className="py-20 px-8 border-t border-zinc-100 bg-white flex flex-col md:flex-row justify-between items-center gap-10">
        <div className="flex items-center gap-4">
           <div className="w-8 h-8 bg-zinc-900 rounded-xl flex items-center justify-center text-white text-[10px] font-serif">C&C</div>
           <span className="text-[10px] font-bold uppercase tracking-[0.5em] text-zinc-300">Camber & Casper Umbrella</span>
        </div>
        <div className="flex gap-12 text-[10px] font-bold uppercase tracking-widest text-zinc-400">
          <a href="#" className="hover:text-zinc-900 transition-colors">Economics</a>
          <a href="#" className="hover:text-zinc-900 transition-colors">Sovereignty</a>
          <a href="#" className="hover:text-zinc-900 transition-colors">Contact</a>
        </div>
      </footer>
    </div>
  );
};

export default App;