import React, { useState, useEffect, useMemo } from 'react';
import { 
  ShieldCheck, 
  MapPin, 
  Lock, 
  ShieldAlert, 
  CheckCircle2, 
  ArrowRight, 
  Globe, 
  Database, 
  ChevronRight,
  Fingerprint,
  Cpu,
  RefreshCw,
  HardDrive,
  EyeOff
} from 'lucide-react';

// --- INTERACTIVE NODE MAP ---
const AustraliaMapNodes = ({ activeNode, setActiveNode }) => {
  const nodes = [
    { id: 'syd', city: 'Sydney', pos: { x: 82, y: 75 }, status: 'Operational', latency: '0.4ms' },
    { id: 'mel', city: 'Melbourne', pos: { x: 74, y: 88 }, status: 'Operational', latency: '0.6ms' },
    { id: 'per', city: 'Perth', pos: { x: 15, y: 65 }, status: 'Optimizing', latency: '1.2ms' },
  ];

  return (
    <div className="relative w-full aspect-square bg-zinc-50 rounded-[40px] border border-zinc-200 overflow-hidden group shadow-inner">
      <div className="absolute inset-0 opacity-[0.05]" style={{ backgroundImage: 'radial-gradient(#000 1px, transparent 1px)', backgroundSize: '30px 30px' }} />
      
      {/* Australia SVG Outline (Minimalist) */}
      <svg viewBox="0 0 100 100" className="w-full h-full p-12 opacity-10">
        <path 
          d="M15,65 L20,70 L30,85 L45,90 L60,95 L75,90 L85,80 L90,65 L85,50 L75,35 L60,20 L45,15 L30,20 L20,35 L15,50 Z" 
          fill="none" 
          stroke="currentColor" 
          strokeWidth="1" 
        />
      </svg>

      {/* Interactive Hotspots */}
      {nodes.map(node => (
        <button
          key={node.id}
          onMouseEnter={() => setActiveNode(node)}
          onMouseLeave={() => setActiveNode(null)}
          className="absolute group/node"
          style={{ left: `${node.pos.x}%`, top: `${node.pos.y}%` }}
        >
          <div className={`relative w-4 h-4 rounded-full transition-all duration-500 border-2 border-white shadow-lg
            ${activeNode?.id === node.id ? 'bg-blue-600 scale-150' : 'bg-zinc-300'}`} />
          <div className={`absolute -top-10 left-1/2 -translate-x-1/2 px-3 py-1 bg-zinc-900 text-white text-[8px] font-bold uppercase tracking-widest rounded-full transition-opacity duration-300
            ${activeNode?.id === node.id ? 'opacity-100' : 'opacity-0'}`}>
            {node.city}
          </div>
        </button>
      ))}

      {/* Floating Meta Data */}
      <div className="absolute bottom-10 left-10 space-y-1">
        <p className="text-[10px] font-bold uppercase tracking-[0.4em] text-zinc-400">Ironbark Infrastructure</p>
        <p className="text-xs font-semibold text-zinc-900">Physical Node Network</p>
      </div>
    </div>
  );
};

// --- ENCRYPTION DEMO ---
const EncryptionDemo = () => {
  const [inputText, setInputText] = useState("Confidential Invoice #1024");
  const [encrypted, setEncrypted] = useState("");

  useEffect(() => {
    // Mock encryption logic
    const chars = "ABCDEF0123456789!@#$%^&*()_+";
    const scramble = inputText.split('').map(() => chars.charAt(Math.floor(Math.random() * chars.length))).join('');
    setEncrypted(scramble);
  }, [inputText]);

  return (
    <div className="bg-white rounded-[40px] p-12 border border-zinc-100 shadow-xl space-y-8">
      <div className="space-y-2">
        <h4 className="text-xl font-semibold tracking-tight">Real-time Synthesis Encryption.</h4>
        <p className="text-sm text-zinc-400 font-light">See how Casper treats every byte of your business data.</p>
      </div>

      <div className="space-y-4">
        <div className="space-y-2">
          <label className="text-[9px] font-bold uppercase tracking-widest text-zinc-300">Plaintext Input</label>
          <input 
            type="text" 
            value={inputText}
            onChange={(e) => setInputText(e.target.value)}
            className="w-full bg-zinc-50 border border-zinc-100 p-4 rounded-2xl text-sm focus:outline-none focus:ring-1 focus:ring-blue-500 transition-all"
          />
        </div>
        <div className="space-y-2">
          <label className="text-[9px] font-bold uppercase tracking-widest text-blue-400">Camber Hardened Output</label>
          <div className="w-full bg-zinc-950 p-4 rounded-2xl text-sm font-mono text-blue-500 break-all min-h-[50px]">
            {encrypted}
          </div>
        </div>
      </div>

      <div className="pt-4 flex items-center gap-3 text-emerald-500">
        <CheckCircle2 size={16} />
        <span className="text-[10px] font-bold uppercase tracking-widest">Quantum-Safe Guard Active</span>
      </div>
    </div>
  );
};

// --- MAIN APP ---

export default function App() {
  const [stage, setStage] = useState('splash');
  const [scrolled, setScrolled] = useState(false);
  const [activeNode, setActiveNode] = useState(null);

  useEffect(() => {
    const timer = setTimeout(() => setStage('main'), 2800);
    const scrollHandler = () => setScrolled(window.scrollY > 40);
    window.addEventListener('scroll', scrollHandler);
    return () => {
      clearTimeout(timer);
      window.removeEventListener('scroll', scrollHandler);
    };
  }, []);

  if (stage === 'splash') {
    return (
      <div className="fixed inset-0 bg-white flex flex-col items-center justify-center z-[100] transition-opacity duration-1000">
        <div className="relative w-64 h-64 flex items-center justify-center animate-in zoom-in duration-1000">
          <svg viewBox="0 0 200 200" className="absolute w-full h-full opacity-10">
            <path d="M40 120C40 86.8629 66.8629 60 100 60C133.137 60 160 86.8629 160 120" stroke="black" strokeWidth="1.5" fill="none" />
          </svg>
          <div className="w-32 h-32 bg-white rounded-[24%] shadow-2xl flex items-center justify-center border border-zinc-100 overflow-hidden">
             <span className="font-serif text-5xl font-light tracking-tighter text-zinc-900">C<span className="text-zinc-300 italic">C</span></span>
          </div>
        </div>
        <div className="mt-12 text-center">
          <p className="text-[10px] tracking-[0.8em] uppercase text-zinc-400 font-bold animate-pulse">Initializing Sovereign Boundary</p>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-[#FDFDFD] text-[#1D1D1F] font-sans selection:bg-zinc-900 selection:text-white transition-opacity duration-1000">
      
      {/* Navigation */}
      <nav className={`fixed top-0 w-full z-50 transition-all duration-700 px-8 ${scrolled ? 'py-4 bg-white/70 backdrop-blur-xl border-b border-zinc-100' : 'py-10'}`}>
        <div className="max-w-7xl mx-auto flex justify-between items-center">
          <div className="flex items-center gap-12">
            <div className="flex items-center gap-3 cursor-pointer group">
              <div className="w-8 h-8 bg-zinc-900 rounded-[24%] flex items-center justify-center text-white text-[10px] font-serif transition-transform group-hover:rotate-12">
                C<span className="text-zinc-400">C</span>
              </div>
              <span className="font-serif text-2xl font-light tracking-tighter">C&C</span>
            </div>
            <div className="hidden lg:flex gap-10 text-[9px] font-bold tracking-[0.3em] uppercase text-zinc-400">
              <a href="#sovereign" className="hover:text-zinc-900 transition-colors">Sovereignty</a>
              <a href="#nodes" className="hover:text-zinc-900 transition-colors">Physical Nodes</a>
              <a href="#audit" className="hover:text-zinc-900 transition-colors">Audits</a>
            </div>
          </div>
          <button className="bg-zinc-900 text-white px-8 py-2.5 rounded-full text-[11px] font-bold tracking-tight hover:bg-zinc-700 transition-all shadow-xl shadow-zinc-100">
            Access Terminal
          </button>
        </div>
      </nav>

      <main className="pt-24">
        {/* HERO */}
        <section id="sovereign" className="min-h-[90vh] flex flex-col items-center justify-center px-6 text-center max-w-6xl mx-auto space-y-12">
          <div className="space-y-6">
            <div className="inline-block px-4 py-1.5 rounded-full bg-blue-50 text-[10px] font-bold tracking-[0.5em] uppercase text-blue-600 border border-blue-100">
              Air-Gapped Australian AI
            </div>
            <h1 className="text-6xl md:text-[110px] font-semibold tracking-tighter leading-[0.9] text-zinc-950">
              Your data stays <br/>
              <span className="text-zinc-300 italic font-light">on our soil.</span>
            </h1>
            <p className="text-xl md:text-2xl text-zinc-400 max-w-2xl mx-auto font-light leading-relaxed">
              Camber & Casper is the only enterprise AI umbrella that is 100% Australian owned, operated, and hosted. No offshore leaks. No global training pools. Just secure intelligence.
            </p>
          </div>
          <div className="flex flex-col sm:flex-row gap-6 justify-center pt-8">
            <button className="bg-zinc-900 text-white px-12 py-5 rounded-full font-bold text-sm tracking-tight transition-all hover:scale-105 shadow-2xl flex items-center gap-3 group">
              View Security Protocols <ArrowRight size={18} className="group-hover:translate-x-1 transition-transform" />
            </button>
            <button className="bg-white border border-zinc-200 text-zinc-900 px-12 py-5 rounded-full font-bold text-sm tracking-tight hover:bg-zinc-50 transition-all">
              Compliance Vault
            </button>
          </div>
        </section>

        {/* NODE MAP SECTION: THE PHYSICAL PROOF */}
        <section id="nodes" className="py-40 px-6 bg-zinc-50">
           <div className="max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-24 items-center">
              <div className="space-y-10">
                 <h2 className="text-sm font-bold tracking-[0.4em] uppercase text-blue-500">Physical Infrastructure</h2>
                 <h3 className="text-5xl font-semibold tracking-tighter">Local Nodes. <br/><span className="text-zinc-300 italic font-light">Global Integrity.</span></h3>
                 <p className="text-lg text-zinc-500 leading-relaxed font-light">
                    Every byte of data processed by Casper sits on physical racks owned by Camber. We don't rent space from tech giants in Silicon Valley. Our Ironbark nodes are located exclusively in Sydney, Melbourne, and Perth.
                 </p>
                 
                 {/* Live Node Telemetry Stats */}
                 <div className="grid grid-cols-1 sm:grid-cols-3 gap-8 p-8 bg-white rounded-[40px] border border-zinc-200">
                    <div className="space-y-1">
                       <div className="text-[9px] uppercase font-bold text-zinc-400 tracking-widest">Active Nodes</div>
                       <div className="text-2xl font-bold">14</div>
                    </div>
                    <div className="space-y-1 border-l border-zinc-100 pl-8">
                       <div className="text-[9px] uppercase font-bold text-zinc-400 tracking-widest">Uptime</div>
                       <div className="text-2xl font-bold">99.998%</div>
                    </div>
                    <div className="space-y-1 border-l border-zinc-100 pl-8">
                       <div className="text-[9px] uppercase font-bold text-zinc-400 tracking-widest">Offshore</div>
                       <div className="text-2xl font-bold text-blue-500">0%</div>
                    </div>
                 </div>
              </div>
              
              <div className="relative">
                <AustraliaMapNodes activeNode={activeNode} setActiveNode={setActiveNode} />
                {activeNode && (
                  <div className="absolute top-10 right-10 p-6 bg-white/80 backdrop-blur-xl border border-white rounded-3xl shadow-xl animate-in fade-in zoom-in duration-300">
                     <div className="flex items-center gap-3 mb-2">
                        <div className="w-2 h-2 bg-green-500 rounded-full animate-pulse" />
                        <span className="text-xs font-bold uppercase tracking-widest">{activeNode.city} Cluster</span>
                     </div>
                     <div className="text-xl font-bold">{activeNode.latency}</div>
                     <div className="text-[9px] text-zinc-400 uppercase font-bold tracking-widest">Response Latency</div>
                  </div>
                )}
              </div>
           </div>
        </section>

        {/* INTERACTIVE ENCRYPTION SECTION */}
        <section className="py-40 px-6 max-w-7xl mx-auto">
           <div className="grid grid-cols-1 lg:grid-cols-2 gap-24 items-center">
              <div className="order-2 lg:order-1">
                 <EncryptionDemo />
              </div>
              <div className="order-1 lg:order-2 space-y-10">
                 <h2 className="text-sm font-bold tracking-[0.4em] uppercase text-zinc-400">Privacy as a Feature</h2>
                 <h3 className="text-5xl font-semibold tracking-tighter">Air-Gapped <br/><span className="text-blue-500 italic font-light">Intelligence.</span></h3>
                 <p className="text-lg text-zinc-500 leading-relaxed font-light">
                   Your business logic is physically separated from the training pools. This means your customer lists, profit margins, and unique trade secrets remain strictly private. We don't train our AI on your success.
                 </p>
                 <ul className="space-y-4 pt-4">
                    {[
                      { icon: <ShieldCheck size={18} />, text: "End-to-end Australian encryption." },
                      { icon: <EyeOff size={18} />, text: "Zero human oversight on raw data." },
                      { icon: <Database size={18} />, text: "Proprietary Ironbark storage architecture." }
                    ].map((item, i) => (
                      <li key={i} className="flex items-center gap-4 text-sm font-bold tracking-tight text-zinc-700">
                        <div className="text-blue-500">{item.icon}</div> {item.text}
                      </li>
                    ))}
                 </ul>
              </div>
           </div>
        </section>

        {/* FINAL CTA: THE TRUST PROMISE */}
        <section className="py-60 px-6 bg-[#0A0A0A] text-white text-center relative overflow-hidden">
           <div className="absolute inset-0 opacity-10 pointer-events-none">
              <svg viewBox="0 0 100 100" className="w-full h-full p-20">
                <circle cx="50" cy="50" r="40" fill="none" stroke="white" strokeWidth="0.1" strokeDasharray="1 4" />
                <path d="M20 50 A 30 30 0 0 1 80 50" fill="none" stroke="white" strokeWidth="0.5" />
              </svg>
           </div>
           <div className="relative z-10 max-w-4xl mx-auto space-y-16">
              <h2 className="text-6xl md:text-[120px] font-semibold tracking-tighter leading-none">Safe. <br/><span className="text-zinc-700 italic font-light">Sovereign.</span></h2>
              <p className="text-xl text-zinc-500 font-light max-w-2xl mx-auto leading-relaxed">
                Trust is the foundation of the Camber & Casper umbrella. Deploy your 100-man team with the peace of mind that your data is protected by the most resilient infrastructure in the country.
              </p>
              <button className="bg-white text-zinc-950 px-16 py-6 rounded-full font-bold text-sm tracking-tight transition-all hover:scale-105 shadow-[0_40px_100px_rgba(255,255,255,0.1)]">
                 Secure Your Organization
              </button>
           </div>
        </section>
      </main>

      <footer className="py-20 px-10 border-t border-zinc-100 bg-white flex flex-col md:flex-row justify-between items-center gap-10">
        <div className="flex items-center gap-4">
           <div className="w-10 h-10 bg-zinc-900 rounded-[24%] flex items-center justify-center text-white text-xs font-serif">C<span className="text-zinc-500 italic">C</span></div>
           <div className="flex flex-col">
              <span className="text-xs font-bold uppercase tracking-[0.4em] text-zinc-900">Sovereign Guard</span>
              <span className="text-[9px] uppercase tracking-widest text-zinc-300">C&C Security Protocol</span>
           </div>
        </div>
        <div className="flex gap-12 text-[10px] font-bold uppercase tracking-widest text-zinc-400">
          <a href="#" className="hover:text-blue-600 transition-colors">Ironbark Nodes</a>
          <a href="#" className="hover:text-blue-600 transition-colors">Compliance</a>
          <a href="#" className="hover:text-blue-600 transition-colors">Support</a>
        </div>
        <div className="text-zinc-300 text-[10px] tracking-widest font-mono">
           © 2024 CAMBER_CASPER_SOVEREIGN
        </div>
      </footer>

      <style dangerouslySetInnerHTML={{ __html: `
        @keyframes shimmer {
          0% { transform: translateX(-100%); }
          100% { transform: translateX(100%); }
        }
      `}} />
    </div>
  );
}